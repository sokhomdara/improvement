name: Keep Bot Alive 24/7

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render to keep bot awake
        run: |
          curl -s https://improvement-5zlq.onrender.com > /dev/null
          echo "Pinged at $(date)"

